{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "scripts": {
    "dev": "yarn --cwd packages/reference devserver",
    "cypress:production": "yarn --cwd packages/reference cypress:production"
  }
}
