{
  "name": "root",
  "private": true,
  "engines": {
    "node": "^11.13.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.13.4"
  },
  "scripts": {
    "dev": "yarn --cwd packages/reference devserver",
    "cy:ci": "yarn --cwd packages/reference node .circleci/cypress.js",
    "build": "yarn --cwd packages/reference build",
    "start": "yarn --cwd packages/reference start",
    "test": "yarn --cwd packages/reference test && yarn --cwd packages/shift-next test && yarn --cwd packages/shift-node-api test && yarn --cwd packages/shift-next-routes test && yarn --cwd packages/shift-react-components test"
  }
}
